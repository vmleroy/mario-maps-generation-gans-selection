# Super Mario Bros. Map Generation using GANs and Heuristic Selection

This project explores the generation of 2D maps for the classic game *Super Mario Bros.* using Generative Adversarial Networks (GANs). It combines a Wasserstein GAN (WGAN) with a heuristic-based selection algorithm to produce high-quality, playable levels that adhere to the original game's design principles. The project is the basis for the paper: "Geração de mapas para jogos em 2D utilizando Redes Generativas Adversariais."

-----

## Key Features

  * **GAN-Based Map Generation**: Utilizes a Wasserstein GAN (WGAN) to create novel map fragments.
  * **Heuristic Selection Algorithm**: Employs a sophisticated fitness function to filter and select the best-generated fragments based on structural coherence.
  * **A\* Playability Agent**: An A\* agent automatically evaluates the playability of each fragment and the final composed maps.
  * **Modular and Customizable**: The architecture allows for easy adjustments to the fitness function, GAN parameters, and evaluation metrics.
  * **Data-Driven**: Trained on the [Video Game Level Corpus (VGLC)](https://github.com/TheVGLC/TheVGLC), ensuring the generated content is grounded in the original game's design.

-----

## Methodology

The map generation process is a multi-stage pipeline designed to ensure both creativity and quality.

### 1\. Data Preprocessing

The textual representations of *Super Mario Bros.* levels from the VGLC are used as the training data. These text files, where each character represents a game tile (e.g., 'X' for ground, 'E' for an enemy), are preprocessed by:

1.  **Label Encoding**: Converting each tile character into a unique numerical value.
2.  **Fragmentation**: The original, full-sized maps are divided into smaller `28x14` samples (fragments) to increase the volume of training data. These are then padded to a `32x32` dimension to match the network's input requirements.

### 2\. WGAN for Fragment Generation

A Wasserstein GAN (WGAN) was chosen for its training stability and ability to avoid common issues like mode collapse. The architecture consists of:

  * **Generator**: Uses ReLU and batch normalization to build map fragments from a latent space vector.
  * **Critic (Discriminator)**: Employs Leaky ReLU and batch normalization to distinguish between real map fragments (from the dataset) and generated ones.

The network was trained to generate **10,000 map fragments**.

### 3\. Heuristic Selection Algorithm

This is the core of the project's quality assurance. After the WGAN generates thousands of fragments, a selection algorithm filters them using a fitness function. This function penalizes structural errors and rewards well-formed patterns. The fitness score is a sum of evaluations for:

  * **Pipes (`C_fit`)**: Checks for the correct number and proper construction of pipes.
  * **Enemies (`I_fit`)**: Assesses the number and placement of enemies.
  * **Holes (`B_fit`)**: Evaluates the quantity and placement of gaps in the terrain.

The algorithm selects the top **1,000 fragments** (10% of the total generated) for the next stage.

### 4\. A\* Agent for Playability Testing

The final step uses an A\* agent to validate playability. The process works as follows:

1.  **Fragment Validation**: The agent tests each of the 1,000 selected fragments individually. Fragments that can be successfully traversed within a 60-second time limit are marked as "playable".
2.  **Map Composition**: The agent constructs full maps by sequentially concatenating 10 fragments. After adding each new fragment, the agent re-evaluates the entire map to ensure it remains playable from start to finish.

-----

## Results

The combination of the WGAN with the heuristic selection algorithm yielded significant improvements over using the WGAN alone.

  * **Enhanced Quality**: The selection algorithm effectively eliminated structural errors like malformed pipes and misplaced enemies, leading to maps with greater structural cohesion and adherence to the original game's patterns.
  * **Improved Playability**: The methodology produced a **25% increase** in the rate of usable, playable fragments.
  * **Faster Evaluation**: The average time required for the A\* agent to evaluate the maps was **reduced by 15%**, indicating that the selected fragments were better structured and easier to navigate.
  * **Balanced Difficulty**: While both methods tended to generate maps on the easier side, the selection algorithm produced a more balanced distribution of difficulties.

|  |  |
| :----------------------------------------------------------: | :----------------------------------------------------------: |
| *Figure 1: Sample maps generated by the pure WGAN, showing structural inconsistencies. * | *Figure 2: Sample maps refined by the selection algorithm, demonstrating improved cohesion. * |

-----

## How to Use

Check `run_scripts.md`.

-----

## Future Work

Future research directions include:

  * **Integrating Evolutionary Algorithms**: Using evolutionary methods to refine the GAN's training process for better pattern learning.
  * **Automating Weight Definition**: Employing heuristic methods like genetic algorithms or tools like Optuna to automatically optimize the fitness function's weights, which were set empirically in this study.
  * **Exploring Advanced Architectures**: Investigating more advanced GANs, such as StyleGAN, to capture more complex design patterns.

-----

## Citation

If you use this project for academic research, please cite the original paper:

[TBD]

## License

This project is licensed under the MIT License. See the `LICENSE` file for more details.

## Acknowledgments

  * **Author**: Victor Le Roy Matos 
  * **Advisor**: Prof. Rogério Martins Gomes 
  * **Co-advisor**: Prof. João Gabriel Gama Vila Nova 
  * This work was developed at the Departamento de Computação, CEFET-MG and published at the Brazilian Symposium on Computer Games and Digital Entertainment (SBGames).
